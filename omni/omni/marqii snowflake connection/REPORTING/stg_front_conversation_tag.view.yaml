# Reference this view as reporting__stg_front_conversation_tag
schema: REPORTING
table_name: STG_FRONT_CONVERSATION_TAG

dimensions:
  conversation_id:
    sql: '"CONVERSATION_ID"'
    format: ID

  tag_id:
    sql: '"TAG_ID"'
    format: ID

  _fivetran_synced:
    sql: '"_FIVETRAN_SYNCED"'
    hidden: true

measures:
  count:
    aggregate_type: count
